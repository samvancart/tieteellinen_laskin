version: '2.1'
orbs:
  python: circleci/python@1.0.0
jobs:
  build:
    executor: python/default
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: poetry 1.2.0
      - run:
          command: |
            poetry run pytest src
          name: Test it
workflows:
  main:
    jobs:
      - build